# ============================================================
# app.alphacure.cloud — Interface cliniques (alphacure-ui)
# ============================================================
server {
    listen 80;
    server_name app.alphacure.cloud;

    # Utilisé par Certbot pour les challenges ACME
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name app.alphacure.cloud;

    ssl_certificate     /etc/letsencrypt/live/app.alphacure.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.alphacure.cloud/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # En-têtes sécurité
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header X-Content-Type-Options nosniff always;

    # Proxy vers l'interface React (alphacure-ui)
    location / {
        proxy_pass http://alphacure-ui:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================================
# admin.alphacure.cloud — Interface administration SaaS (alphacure-admin)
# ============================================================
server {
    listen 80;
    server_name admin.alphacure.cloud;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name admin.alphacure.cloud;

    ssl_certificate     /etc/letsencrypt/live/admin.alphacure.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.alphacure.cloud/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header X-Content-Type-Options nosniff always;

    # Proxy vers l'interface React (alphacure-admin)
    location / {
        proxy_pass http://alphacure-admin:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ============================================================
# kk.alphacure.cloud — Keycloak IAM
# ============================================================
server {
    listen 80;
    server_name kk.alphacure.cloud;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name kk.alphacure.cloud;

    ssl_certificate     /etc/letsencrypt/live/kk.alphacure.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kk.alphacure.cloud/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Proxy vers Keycloak
    location / {
        proxy_pass http://alphacure-keycloak:8180;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port 443;
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
    }
}

# ============================================================
# api.alphacure.cloud — API Gateway (interne — optionnel mais recommandé)
# ============================================================
# ATTENTION: L'API Gateway n'a pas besoin d'être exposé publiquement
# si les UI y accèdent via app.alphacure.cloud (proxy CORS via Nginx).
# Activez cette section uniquement si vous avez des clients mobiles
# ou des intégrations tierces qui appellent l'API directement.
#
# server {
#     listen 80;
#     server_name api.alphacure.cloud;
#     location / { return 301 https://$host$request_uri; }
# }
#
# server {
#     listen 443 ssl;
#     server_name api.alphacure.cloud;
#     ssl_certificate     /etc/letsencrypt/live/api.alphacure.cloud/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.alphacure.cloud/privkey.pem;
#     location / {
#         proxy_pass http://gateway-service:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
